# SCRIPTMETAKit 指摘事項レビューと改良計画

- 作成日: 2026-07-10
- 参照: `20260710指摘事項.md`
- 対象: `SCRIPTMETAKit` main / tag 1.0.8 と現在の未コミット変更
- 利用側: Scripta!、ACEMenuPlus

## 結論

`20260710指摘事項.md` の主要な指摘は妥当です。特に、FFI の排他参照、Workspace の actor 再入、dirty refresh の部分結果反映、metadata 編集時のファイル属性消失は、データ破損または予測不能な動作につながるため、次の Kit 公開前に修正する必要があります。

ただし、修正順は元の一覧どおりに一件ずつ進めるより、次の単位へ組み替える方が安全です。

1. 既存変更を分離し、再現テストを先に追加する
2. 小さく独立した境界値・解析・プラットフォーム修正を行う
3. FFI、Swift Task、Workspace を一つのキャンセル設計として修正する
4. 走査結果、編集、バックアップを「成功時だけ確定する」処理へ変更する
5. Rust、C FFI、C header、Swift、XCFramework、利用アプリを同じ状態で検証する

Scripta! または ACEMenuPlus 側へ一時的な代替処理を追加して回避する方針にはしません。共通動作は Kit の Rust コアと公開 Swift / FFI API で修正し、アプリ側には UI、表示、実行時の方針だけを残します。

## 現在の利用状況

### 共通の依存状態

Scripta! と ACEMenuPlus は、どちらも次の公開済み Kit を解決しています。

- Version: `1.0.8`
- revision: `f764d80bb010b6175702d1c97c29d3bba64b62e8`
- Kit の tag 1.0.8 と現在の HEAD も同じ revision
- 追跡済み XCFramework の dylib SHA-256: `1f2cdcfba00265e7d9acd66a37ce29aea7fde49266788bca5b44105067c55e39`

したがって、公開済み 1.0.8 を使用中の両アプリが、現在の未コミット Rust コードと古い XCFramework を混在させて実行しているわけではありません。P1-10 は、未コミット変更を次の Version として公開する前に必ず解消すべき配布工程上の問題です。

両アプリの Swift Package 条件は上限が `999.0.0` の version range です。将来の破壊的な major Version も解決対象になるため、次の Kit 統合作業時に `upToNextMajorVersion` 相当へ変更します。`Package.resolved` の更新だけに安全性を依存させません。

### Scripta! が使用している機能

Scripta! は一つの共有 `ScriptMetaKitWorkspace` を次の用途に使用しています。

- metadata catalog の全走査と一部 root の再走査
- file list と persistent cache
- native watcher と dirty-only refresh
- 更新確認と明示的キャンセル
- SCRIPTMETA の read、write、preview
- backup の作成、復元、履歴初期化
- macOS alias を含む登録フォルダの走査

このため、P1-1、P1-3、P1-4、P1-5、P1-8、P1-10、P2-2、P2-4、P2-6、P2-8、P2-9 は実際の機能経路に関係します。

特に `scanMetadataContexts()` は、root 全体の登録、一部 root の走査、全 root の cached result 取得を三つの Workspace 呼び出しに分けています。Workspace 内部の各メソッドだけを直列化しても、この三段処理全体の割り込みは防げません。Kit に一つの複合 API を追加し、Scripta! はそれを呼ぶ形へ変更する必要があります。

### ACEMenuPlus が使用している機能

ACEMenuPlus は次の用途に Kit を使用しています。

- 登録フォルダの metadata 走査
- persistent catalog cache
- 更新確認
- 進捗表示と明示的キャンセル
- macOS alias を含む登録フォルダの走査

更新確認では、現在は script ごとに独立した `ScriptMetaKitEngine()` を作り、Task group で並行実行しています。一方、キャンセル先は Workspace が保持する engine だけです。このため、画面上の Task をキャンセルしても個別 engine の HTTP / file I/O が継続し得ます。P2-2 は ACEMenuPlus では次回公開前の修正対象です。

ACEMenuPlus 側の更新確認は、Kit が持つ batch `checkUpdates(items:)` と Kit 内部の並行数制御を使用する形へ変更します。アプリ側で engine を複数生成して並行数とキャンセルを管理しません。

### 判断の根拠とした主なコード

- Kit の複合操作: [`SCRIPTMETAKit/Sources/ScriptMetaKit/Workspace.swift`](SCRIPTMETAKit/Sources/ScriptMetaKit/Workspace.swift)
- Swift async / cancel 境界: [`SCRIPTMETAKit/Sources/ScriptMetaKit/Engine.swift`](SCRIPTMETAKit/Sources/ScriptMetaKit/Engine.swift)
- Rust FFI handle: [`SCRIPTMETAKit/scriptmetakit_ffi/src/lib.rs`](SCRIPTMETAKit/scriptmetakit_ffi/src/lib.rs)
- Scripta! の共有 Workspace: [`Scripta/ScriptMetaKitBridge.swift`](../Scripta/Scripta/ScriptMetaKitBridge.swift)
- Scripta! の watcher: [`Scripta/FileListMonitor.swift`](../Scripta/Scripta/FileListMonitor.swift)
- Scripta! の editor / backup: [`Scripta/ScriptMetaEditorSupport.swift`](../Scripta/Scripta/ScriptMetaEditorSupport.swift)
- ACEMenuPlus の Workspace と個別 engine 更新確認: [`ACEmenu/ScriptMetaKitIntegrationSupport.swift`](../ACEMenu/ACEmenu/ACEmenu/ScriptMetaKitIntegrationSupport.swift)
- ACEMenuPlus の Task cancel: [`ACEmenu/ScriptUpdatePanelStoreWorkflowSupport.swift`](../ACEMenu/ACEmenu/ACEmenu/ScriptUpdatePanelStoreWorkflowSupport.swift)

## 指摘事項の再評価

| ID | 判断 | 利用側への影響 | 計画上の扱い |
| --- | --- | --- | --- |
| P1-1 | 妥当 | 両アプリの明示的キャンセルから到達可能 | P2-2 と統合し、次回公開前に修正 |
| P1-2 | 妥当 | macOS アプリへは直接影響しないが Windows 対応 Kit の安全性に必須 | 次回公開前に修正 |
| P1-3 | 妥当 | Scripta! の共有 Workspace と ACEMenuPlus の Workspace 差し替えに影響 | 次回公開前に修正 |
| P1-4 | 妥当 | Scripta! の watcher / dirty-only refresh で既存表示を失い得る | 次回公開前に修正 |
| P1-5 | 妥当 | Scripta! の editor が利用者のファイル属性を失わせ得る | 次回公開前に修正 |
| P1-6 | 妥当 | Swift Workspace の既定値は `true` だが、Rust 公開設定の契約違反 | 次回公開前に修正 |
| P1-7 | 妥当 | 両アプリの現在値は enabled だが、通信無効化の契約違反 | 次回公開前に修正 |
| P1-8 | 妥当 | Scripta! editor の Script-ID 変更失敗に直結 | P2-5 と統合し、次回公開前に修正 |
| P1-9 | 妥当 | 両アプリが Swift Package として利用 | 最初の小修正に含める |
| P1-10 | 妥当。ただし公開済み 1.0.8 の実行不一致ではない | 次の未コミット変更を含む artifact 生成時に影響 | release gate として扱う |
| P2-1 | 妥当 | Scripta! の preview API で将来誤入力時に全量読込 | 小修正として同時対応 |
| P2-2 | 妥当。優先度を引き上げる | 両アプリの Task cancel、特に ACEMenuPlus の個別 engine に影響 | P1-1 と同時に修正 |
| P2-3 | 妥当 | 現行アプリは設定変更していない | API 整理段階で修正 |
| P2-4 | 妥当。優先度を引き上げる | Scripta! watcher 後に古い Script-ID / Version が残り得る | P1-4 と同時に修正 |
| P2-5 | 妥当 | Scripta! の edit state と scan 判定が不一致 | P1-8 と共通 parser で修正 |
| P2-6 | 妥当。優先度を引き上げる | 両アプリが不完全な catalog を完全成功と誤認し得る | snapshot 確定処理と同時に修正 |
| P2-7 | 妥当 | Scripta! の file list / metadata、両アプリの update check root 統合に影響 | 同じ修正版で対応 |
| P2-8 | 妥当。優先度を引き上げる | Scripta! の backup browser で既存履歴を失い得る | 次回公開前に修正 |
| P2-9 | 妥当 | 未コミット alias 変更をそのまま取り込むと両アプリの走査負荷が増える | alias API 公開と同時に修正 |
| P2-10 | 妥当 | 両アプリの対応対象ファイルを読み落とし得る | 解析修正に含める |
| P2-11 | 妥当 | 利用アプリ実行には直接影響しない | 開発スクリプト整備で修正 |
| P2-12 | 妥当 | 両アプリが取り込む XCFramework の再現性に影響 | release gate として修正 |
| P2-13 | 妥当 | 現在の Swift アプリは wrapper 経由だが、公開 C FFI の安全性に必要 | 次の header 配布前に修正 |
| P2-14 | 妥当 | 現在の macOS 版二アプリには直接影響しない | 未対応を明記し、Windows 計画へ分離 |
| 追加指摘 | 妥当 | status の誤分類で利用側のエラー判断を誤らせる | 数値直書きを廃止して同時修正 |

## 改良方針

### 1. 公開互換性を維持する

次の修正版は `1.0.9` 候補として扱い、既存の Swift public API と C symbol を削除または変更しません。必要な API は追加に限定します。

- `ScriptMetaKitEngine.cancelCurrentOperation()` は残す
- `ScriptMetaKitWorkspace` の既存走査 API は残す
- C ABI の既存 handle と free 関数を残す
- Rust の公開 enum の数値を変更しない
- 新しい動作をアプリ側だけの代替処理で実装しない

破壊的変更が必要になった場合は 1.0.9 へ含めず、2.0 の別計画に分けます。

### 2. 長時間処理は一つの所有者と一つのキャンセル単位を持つ

FFI handle 内部を、可変 engine 全体への裸の `&mut` と atomic cancellation の並存から、次の構造へ変更します。

- 長寿命の outer handle
- `Mutex<EngineState>` で保護する engine、last error、watcher
- outer handle から共有参照で触れる cancellation state
- 操作ごとに識別できる cancellation token または operation ID
- 通常の FFI 操作は handle 内部で直列化

Swift 側では各 async API を `withTaskCancellationHandler()` で囲みます。親 Task がキャンセルされた時点で、その Task が開始した operation だけを Kit へ通知します。待機中の別操作まで取り消す global cancel だけには依存しません。

Workspace には、複数の `await` を含む一連の処理を最後まで直列化する非再入ゲートを置きます。ただしキャンセル要求はゲートの外から実行できるようにし、走査完了待ちによる deadlock を避けます。単に `@MainActor` を付けたり、`@unchecked Sendable` を追加したりする修正にはしません。

### 3. 状態更新は成功時だけ確定する

走査、ファイル編集、backup 初期化は、途中の状態を現在値へ直接上書きせず、次の共通手順へ変更します。

1. 一時状態へ処理結果を作る
2. cancel、timeout、overflow、I/O error を判定する
3. 完全成功した範囲だけ現在状態へ反映する
4. 失敗時は旧 snapshot、dirty state、backup 履歴を保持する
5. 部分結果が必要な場合は戻り値としてだけ返し、永続 cache へ確定しない

## 実装計画

### Phase 0: 作業開始前の分離と再現テスト

現在の未コミット変更は次の四ファイルです。

- `SCRIPTMETAKit/src/lib.rs`
- `SCRIPTMETAKit/src/scanner/mod.rs`
- `SCRIPTMETAKit/src/scanner/path_resolution.rs`
- `SCRIPTMETAKit/tests/package_smoke.rs`

alias 公開 API と JSDoc 読み取りテストが混在しているため、修正作業へ入る前に目的別の変更へ分離します。既存変更を破棄または上書きしません。

各問題は修正前に失敗する最小テストを用意します。特に次を先に固定します。

- progress callback 内と別 thread からの FFI cancel
- 同じ groupID への並行 `scanRoots()`
- 遅いローカル Meta-URL の親 Task cancel
- dirty refresh の cancel / timeout / overflow
- JSDoc の各行に `* ` が付く実際の形式
- executable mode、xattr、ACL、read-only
- backup 初期化の新規 backup 作成失敗
- symlink の root、directory、file
- case-sensitive filesystem 上の `swift build` と `swift test`

### Phase 1: 小さく独立した修正

大きな並行処理変更より先に、影響範囲が限定された次の修正を行います。

1. `Package.swift` の test target に `path: "tests/ScriptMetaKitTests"` を指定する
2. Windows watcher の UTF-16 読み取りを `u16::from_le_bytes()` または同等の安全な復号へ変更する
3. `maxBytes < 0` と各上限値を Swift、C FFI、Rust の全境界で拒否する
4. status の数値直書きを型付き定数へ置き換える
5. `update_check.enabled == false` なら resolver を作る前に終了し、通信しない
6. `follow_symlinks == false` では `symlink_metadata()` を基準にリンク先を開かない
7. comment prefix の正規化を parser、marker 検出、editkit で共有する
8. BOM なし UTF-16 heuristic を legacy Japanese encoding より先に判定する

JSDoc は read だけでなく、preview、write、再 parse 後の Script-ID まで同じテストで確認します。

### Phase 2: FFI、Swift Task、Workspace のキャンセル統合

#### Rust / C FFI

- `SmkEngine` を outer handle と locked state に分ける
- `engine_mut()` が FFI 呼び出し全体へ無制限な `&mut SmkEngine` を返す構造を廃止する
- cancel は locked engine 本体ではなく、独立した cancellation state へ共有参照で通知する
- 通常操作、last error、watcher の同時利用契約をコードと C header に明記する
- operation ID を使い、対象外の待機操作を取り消さないことをテストする

#### Swift wrapper

- `Task.detached().value` だけの実装を cancellation handler 付きの共通 helper へまとめる
- 親 Task cancel を対象 operation の FFI token へ伝える
- `ScriptMetaKitEngine` の `@unchecked Sendable` は、内部同期がテストで証明できる状態になってから維持可否を判断する
- callback からの cancel、外部 Task からの cancel、deinit / shutdown の競合を検証する

#### Workspace

- configure、root 更新、cache load、scan、cache save を一つの非再入区間として扱う
- `clearVolatileState()` と `shutdown()` も同じ状態遷移規則へ含める
- cancel は非再入ゲートを待たず、現在の operation へ通知できるようにする
- 同じ groupID への A / B 並行走査で、A が B の root を使わない回帰テストを追加する

#### Scripta! 用の Kit API

Scripta! の一部 root 再走査で必要な次の処理を、一つの Workspace 公開 API にまとめます。

1. catalog 全 root の登録
2. 指定 root だけの再走査
3. catalog 全 root の結果返却
4. cache の成功時保存

具体的な API 名は実装時に既存命名へ合わせますが、三つの公開 API をアプリ側で順番に呼ぶ構成は残しません。

#### ACEMenuPlus の利用変更

- script ごとの `ScriptMetaKitEngine()` と app-side Task group を廃止する
- 一つの managed Workspace / Engine の `checkUpdates(items:)` を使う
- Kit の `max_concurrent_meta_url_checks` と operation cancellation を使用する
- folder scan と update check が重なった場合も、どの operation をキャンセルしたか識別できるようにする

### Phase 3: catalog、編集、backup のトランザクション化

#### dirty refresh

- dirty directory の新 snapshot を一時領域に作る
- `RootStatus::Ready` の場合だけ旧 record と入れ替える
- cancelled、timed out、overflowed、partial、nested I/O error では旧 snapshot と cache を保持する
- 未完了時は dirty state を残し、次回 `refresh_dirty_roots()` で再試行できるようにする
- dirty directory 内では size と mtime だけによる record 再利用を行わない
- 通常走査の再利用を残す場合は `FileIdentity` と高精度 timestamp を含める

`OperationStatus::Partial` を実際に使用し、overflow と読み取れない subtree を完全成功として返しません。Scripta! と ACEMenuPlus は partial を表示用 notice または再試行条件へ変換します。

#### metadata write

- 同じ directory に一時ファイルを作る
- 元ファイルの mode と実行権限を必ず保持する
- macOS では ACL、xattr、Finder tag、lock 関連属性を保持する
- write API 内で read-only / locked を再確認する
- 一時ファイルへの書込、属性適用、必要な同期が成功してから rename する
- rename 失敗時は元ファイルと backup を保持する

プラットフォーム固有属性は helper 層へ分離し、macOS 対応を他 OS の曖昧な模倣へ置き換えません。

#### backup reset

- 新 generation を一時 directory に完成させる
- 内容と index の書込成功後に旧履歴と入れ替える
- source 消失、権限、容量、I/O error の各失敗で旧履歴が残ることを確認する

### Phase 4: API と走査規則の整合

- `ParserOptions` を実処理へ接続するか、`ScannerOptions` へ一本化する
- marker の開始、終了、comment prefix、大小文字規則を一つの parser helper にする
- root purpose の統合を `includes_file_list()` と `includes_metadata()` の論理結果から決める
- `UpdateCheck` と `FileList` の統合を引数順に依存させない
- standalone path resolution だけは全拡張子の macOS alias を probe できるようにする
- 通常 scanner は extension filter 後に必要な alias だけを probe する
- `resolve_registered_path()` を Rust だけで終わらせず、C FFI、C header、Swift wrapper、Swift test まで同じ変更に含める

`PathKind::WindowsShortcut` は、次の修正版では未対応であることを README と API 文書へ明記します。.lnk 解決を実装する場合は Windows 実機テストを必須とする別作業にします。

### Phase 5: artifact と配布工程

`build_xcframework.sh` 自体を先に安全化してから新 artifact を生成します。

- 既存 artifact を最初に削除しない
- 一時 directory に universal dylib と XCFramework を完成させる
- release 用 feature set を script 内で固定する
- architecture、symbol、header、HTTP、native watcher の smoke test を通す
- 成功後だけ既存 artifact と原子的に入れ替える
- Version、Git revision、features、dylib SHA-256 を機械検証できる manifest を作る
- binary 内の個人パスと不要 symbol を再確認する

`build_and_run.sh` は、実在する example を追加するか、現在の API を使う smoke command へ変更します。存在しない `scan_folder` は呼びません。

C header には全 result accessor 共通の ownership、lifetime、free、thread safety、callback reentrancy の節を追加します。

## 利用アプリでの検証計画

### Scripta!

1. 起動時の cached catalog 表示
2. 全登録フォルダ走査
3. 一部 root 再走査中に別の再走査要求が来る場合
4. file list watcher の連続 event と dirty-only refresh
5. timeout / cancel 後に旧一覧が残り、次回再試行されること
6. 更新確認の開始、進捗、キャンセル、再実行
7. JSDoc 形式の read、Script-ID 変更、保存、再読込
8. 0755 ファイル、Finder tag、xattr、ACL、read-only ファイルの編集
9. backup reset 失敗後も旧履歴から復元できること
10. symlink と macOS alias を含む登録フォルダ
11. Swift 6 complete strict concurrency と MainActor default isolation での build

### ACEMenuPlus

1. 登録フォルダ走査と cache 復元
2. 全 script 更新確認
3. 一件だけの更新確認
4. 更新確認直後と途中のキャンセル
5. キャンセル後に HTTP / file I/O が継続しないこと
6. folder scan と update check の連続開始
7. background 定期確認
8. Desktop 上の登録フォルダで新しい権限要求経路を増やさないこと
9. alias / symlink を含む登録フォルダ
10. Swift 6 complete strict concurrency での build

### 配布物

- 両アプリの `Package.resolved` が同じ次期 Kit revision を指す
- Swift Package の Version 条件を次の major 未満へ制限する
- build された各 `.app` 内の `libscriptmetakit_ffi.dylib` が arm64 / x86_64 universal である
- packaged dylib の SHA-256 が Kit release manifest と一致する
- Kit の source test 成功だけでなく、実際に格納した XCFramework 経由の Swift test を成功させる

## CI の必須項目

- `cargo test --workspace --all-features`
- `cargo test --workspace --no-default-features`
- `cargo clippy --workspace --all-targets --all-features -- -D warnings`
- `cargo fmt --all -- --check`
- Windows runner で native watcher test
- Swift Package test
- Swift 6 complete strict concurrency、warnings as errors
- arm64 / x86_64 build
- case-sensitive filesystem 上の `swift build` と `swift test`
- XCFramework の architecture、header、公開 symbol、固定 feature smoke test
- source revision と artifact manifest の一致
- Scripta! と ACEMenuPlus の build と対象 integration test

## 公開手順

1. Kit の既存未コミット変更を目的別に分離する
2. Phase 1 から Phase 4 を小さな commit 単位で実装する
3. source test と Windows / case-sensitive test を通す
4. 安全化した script で XCFramework を生成する
5. artifact test と manifest 検証を通す
6. tag 前の Kit branch を Scripta! と ACEMenuPlus へローカル統合する
7. 両アプリの利用経路と packaged dylib を検証する
8. Kit の 1.0.9 候補を tag / 公開する
9. 両アプリの Package 条件と `Package.resolved` を更新する
10. 両アプリを再 build し、配布 bundle 内の dylib を再確認する

Kit の tag を先に公開してから利用アプリで問題を見つける順序にはしません。tag 前の同一 commit と artifact を両アプリで検証し、その検証済み commit を tag にします。

## 完了条件

- 元の P1 と、本書で優先度を引き上げた P2-2、P2-4、P2-6、P2-8 が修正されている
- cancel、timeout、overflow、I/O error で旧 catalog と backup 履歴を失わない
- Scripta! の metadata 編集で mode、xattr、ACL を失わない
- ACEMenuPlus の更新確認が一つの Kit 管理 operation としてキャンセルできる
- Workspace の同一 group 並行再現テストで要求が混ざらない
- Rust、C FFI、C header、Swift wrapper、XCFramework の機能と Version が一致する
- case-sensitive filesystem と Windows watcher の検証が成功する
- Scripta! と ACEMenuPlus の実利用経路を、同じ Kit commit と artifact で検証済みである
- 利用アプリ側に Kit の不足を補う重複 parser、scanner、cancel 処理を追加していない

## 実装結果（2026-07-11）

計画を `1.0.9` 候補として実装しました。既存 public API と C symbol は維持し、必要な機能は追加 API と内部動作の修正で対応しています。

### 指摘事項への対応

| ID | 結果 | 主な対応 |
| --- | --- | --- |
| P1-1 / P2-2 | 対応済み | FFI handle を outer cancellation と `Mutex<SmkEngineState>` に分離。Swift は operation ticket、cancellation handler、直列 gate を使用し、親 Task cancel と callback cancel をテスト |
| P1-2 | 対応済み | Windows watcher の UTF-16 file name を alignment 非依存の `u16::from_le_bytes()` で復号 |
| P1-3 | 対応済み | Workspace に非再入 gate を追加。group 置換、一部走査、全 catalog 返却を一つにした `scanRegisteredRoots` を追加 |
| P1-4 / P2-4 / P2-6 | 対応済み | dirty refresh の旧 snapshot 保持、dirty 維持、同一 size / mtime の再解析、overflow / unreadable の `Partial` 化と再試行を実装 |
| P1-5 | 対応済み | write 前の read-only / lock 再確認、mode 保持、macOS ACL / xattr 保持、同一 directory の一時ファイル置換を実装 |
| P1-6 | 対応済み | `follow_symlinks=false` では root、directory、file の symlink を追跡しない規則へ統一 |
| P1-7 | 対応済み | `update_check.enabled=false` を resolver 作成前に拒否 |
| P1-8 / P2-5 | 対応済み | parser、marker 検出、editkit で comment prefix 正規化を共有し、`* ` 付き JSDoc の置換をテスト |
| P1-9 | 対応済み | Swift test target の実 path を明示し、case-sensitive APFS 上で build と24テストを完走 |
| P1-10 / P2-12 | 対応済み | 固定 feature の staged XCFramework 生成、成功後の入れ替え、source tree / dylib hash manifest を追加して artifact を再生成 |
| P2-1 / 追加指摘 | 対応済み | 負の `maxBytes` と16 MiB超を Swift / FFI で `INVALID_ARGUMENT` として拒否し、status 数値直書きを廃止 |
| P2-3 | 対応済み | `ParserOptions.max_prefix_bytes` と `require_closed_block` を metadata scan と公開 parser API へ接続 |
| P2-7 | 対応済み | root purpose を `includes_file_list()` / `includes_metadata()` の論理結果から順序非依存で統合 |
| P2-8 | 対応済み | backup reset を新 generation 完成後の directory swap に変更。作成失敗時の旧履歴保持をテスト |
| P2-9 | 対応済み | 通常 scanner の alias probe を extension filter 後に限定し、standalone path resolution だけ全拡張子を対象化 |
| P2-10 | 対応済み | BOM なし UTF-16 LE / BE heuristic を legacy Japanese encoding より先に実行 |
| P2-11 | 対応済み | 存在しない `scan_folder` example を廃止し、実在する package smoke command へ変更 |
| P2-13 | 対応済み | C header に result slice、free、thread safety、callback reentrancy の共通契約を追加 |
| P2-14 | 計画どおり明記 | Rust / C FFI / Swift の path resolution API を追加。`.lnk` 解決は未実装であることを README に明記 |

### 利用アプリへの反映

- Scripta! の一部 root 再走査を、三つの Workspace 呼び出しから Kit の `scanRegisteredRoots` 一回へ変更
- ACEMenuPlus の script ごとの `ScriptMetaKitEngine()` と Task group を廃止し、共有 Workspace の batch `checkUpdates(items:)` 一回へ変更
- 両 Xcode project の Kit 条件を `1.0.9` から次の major 未満へ変更

公開前のローカル Git packageを使い、Scripta! と ACEMenuPlus をそれぞれ Debug / arm64 で buildしました。両 `.app` 内の `libscriptmetakit_ffi.dylib` は `x86_64 arm64` universalで、SHA-256 は Kit manifest の `a323020b8b268b69bccfe4a4fb15903a1dcba730e5768b5980ed198b7a16a9de` と一致しました。

### 最終検証

- `cargo test --workspace --all-features`: Rust 70、package smoke 73、FFI unit 6、FFI smoke 21、全成功
- `cargo test --workspace --no-default-features`: Rust 70、package smoke 71、FFI unit 6、FFI smoke 19、全成功
- `cargo clippy --workspace --all-targets --all-features -- -D warnings`: 成功
- `swift test -Xswiftc -strict-concurrency=complete -Xswiftc -warnings-as-errors`: 24件成功
- case-sensitive APFS 上の同じ Swift test: 24件成功
- Windows `x86_64-pc-windows-msvc` の `native-watch` cross-check（warnings denied）: 成功
- XCFramework: `x86_64 arm64`、公開 symbol、配布 header、個人 absolute path 非混入を確認
- Scripta! / ACEMenuPlus: `1.0.9` ローカル packageを使用した build成功、格納 dylib hash一致

Windows の `blocking-http` を含む全 feature cross-check は、macOS host に MSVC の C 標準 header がなく、依存する `ring` の `assert.h` 検出で停止しました。Windows watcher 本体のコンパイルは成功していますが、Windows 実機での runtime test は CI の最終 release gate として残します。

### 公開前に残る作業

現在の artifact manifest は、未コミットの候補を正しく示すため `sourceState: dirty` と source tree hash を記録しています。公開時は次の順序を守ります。

1. Kit、Scripta!、ACEMenuPlus の変更を reviewして commitする
2. cleanな Kit commitで XCFrameworkを再生成し、manifestを `sourceState: clean` にする
3. Windows CIを完走する
4. 検証済み Kit commitへ `1.0.9` tagを付けて公開する
5. 両アプリの `Package.resolved` を公開済み `1.0.9` revisionへ更新し、最終 buildする
